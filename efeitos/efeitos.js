/**
 * Funcão responavel por ativar a tab de resultado, toda vez que uma consulta for realizada.
 */
function ativarTab(){
	$('#mytab li:eq(1) a').tab('show'); // Select third tab (0-indexed)
}

/**
 * Funcão para realçar as palavras chaves da consulta.
 */
function realcaPalavraChave(){
	
}
